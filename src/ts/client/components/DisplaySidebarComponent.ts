import Vue from 'vue';

export const DisplaySidebarComponent = Vue.extend({
    data: () => ({
    }),
    template: `
    <div class="sidebar">
    </div>
`,
    mounted: function() {

    }
});
